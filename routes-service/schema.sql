-- El routes-service solo consulta tablas existentes en la DB de PostgreSQL creadas por lines-service.
-- Este archivo queda vacío para evitar errores de inicialización, ya que no tiene que crear tablas propias.
SELECT 1; 
